<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Agenda_App.MainPage">

    <!--<ScrollView>
        <VerticalStackLayout
            Padding="30,0"
            Spacing="25">
            <Image
                Source="agenda_pic.jpg"
                HeightRequest="185"
                Aspect="AspectFit"
                SemanticProperties.Description="455868-PFJZTR-616.jpg by FreePik" />

            <Label
                Text="Welkom!"
                Style="{StaticResource Headline}"
                SemanticProperties.HeadingLevel="Level1" />

            <Label
                Text="Welcome bij Agenda &#10; de .NET Multi-platform demonstratie App UI voor het vak .Net Project"
                Style="{StaticResource SubHeadline}"
                SemanticProperties.HeadingLevel="Level2"
                SemanticProperties.Description="Welcome to dot net Multi platform App U I" />

            <Button
                x:Name="btnAppointments"
                Text="Toon afspraken" 
                SemanticProperties.Hint="Opent de afsprakenpagina"
                HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>-->

    <Grid RowDefinitions="35, 150, 120, Auto, *" ColumnDefinitions=".75*,0.25*" Padding="10">
        <Image Grid.Row="1" Grid.ColumnSpan="2" Source="agenda_pic.jpg" BackgroundColor="Bisque"/>

        <StackLayout Grid.Row="2" Margin="10">
            <Entry Placeholder="Wanneer?" />
            <Entry Placeholder="Wat?"/>
        </StackLayout>

        <Button Grid.Row="2" Grid.Column="1" Text="Niew" Margin="10"/>
        <CollectionView Grid.Row="3" Grid.ColumnSpan="2">
            <CollectionView.ItemsSource>
                <x:Array Type="{x:Type x:String}">
                    <x:String>15/11 09:30 Dokter</x:String>
                    <x:String>17/11 12:00 Uit eten met baas</x:String>
                </x:Array>
            </CollectionView.ItemsSource>
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItem Text="Verwijder" BackgroundColor="Red" />
                        </SwipeView.RightItems>
                        <Grid HeightRequest="50" Padding="0,2">
                            <Border VerticalOptions="Center">
                                <Label Text="{Binding .}" Margin="10" FontSize="18"/>
                            </Border>
                        </Grid>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </Grid>
</ContentPage>
